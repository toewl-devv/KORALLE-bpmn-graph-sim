# Roadmap
- [x]  Add Gateway types (i.e. XOR, OR, AND gates). Not sure yet how to decide which option is given, for now I will do equal probability.
- [x]  Make time step lengths configurable, e.g. time_step += 0.1, 1, 0.5, etc.
- [ ]  Use a better clock than sleep(1.0 / timescale)
- [ ]  Finally, make visualisation better
