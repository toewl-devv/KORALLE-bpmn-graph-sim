# Roadmap
1. Add Gateway types (i.e. XOR, OR, AND gates). Not sure yet how to decide which option is given, for now I will do equal probability.
2. Make time step lengths configurable, e.g. time_step += 0.1, 1, 0.5, etc.
3. Use a better clock than sleep(1.0 / timescale)
4. Finally, make visualisation better
